# TaskBoard Debug Benchmark

A deliberately small but multi-file Python project with several logic defects.

Rules for the coding agent:

- Do not modify `verify.py`.
- Preserve the public function names.
- Fix the implementation rather than hard-coding verifier output.
- Use only the Python standard library.
- Finish by running `python verify.py` and getting a PASS.
